# =========================================================
# 🌐 LOCAL ENVIRONMENT
# =========================================================
NODE_ENV=development
PORT=3000

# Base URL of your local Next.js server
VITE_RESULT_BASE_URL="http://localhost:3000"

# Ngrok webhook endpoint from your running n8n tunnel
VITE_N8N_WEBHOOK_URL="https://indiscernibly-biochemic-agnes.ngrok-free.dev/webhook/commet_generate_logo"
N8N_WEBHOOK_URL="https://indiscernibly-biochemic-agnes.ngrok-free.dev/webhook/commet_generate_logo"

# PostgreSQL (Aiven)
# NOTE: Do NOT commit credentials to git. Use environment variables in your host (Vercel/Aiven) instead.
# The DATABASE_URL below is a placeholder. Replace it locally with your real URL in a private .env that is NOT committed.
DATABASE_URL="postgres://avnadmin:REDACTED@pg-177146a0-adityakammati3-9e2c.i.aivencloud.com:20258/defaultdb?sslmode=require"

# Optional local cache directory for job results
RESULTS_DIR="./public/results"

# Logging
LOG_LEVEL=debug
